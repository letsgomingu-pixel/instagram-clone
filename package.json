{
  "name": "my_instagram",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "i am not a fishmonger — React + FastAPI full-stack app",
  "scripts": {
    "setup": "node scripts/setup.js",
    "dev": "node scripts/dev.js",
    "start": "npm run dev",
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "e2e:report": "playwright show-report"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
