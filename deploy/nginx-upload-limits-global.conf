# Global upload limits for iamnotafishmonger.com (installed to /etc/nginx/conf.d/).
# Included inside nginx http {} — applies to every virtual host unless overridden.
client_max_body_size 200M;
client_body_timeout 300s;
proxy_read_timeout 300s;
proxy_send_timeout 300s;
